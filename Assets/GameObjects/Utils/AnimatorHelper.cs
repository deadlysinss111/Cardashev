using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class AnimatorHelper
{
    static Dictionary<int, AnimationClip> hashToClip = new();

    //static List<int> AddAnimatorHash(Animator animator)
    //{
    //    for (int i = 0; i < animator.layerCount; i++)
    //    {
    //        AnimatorClipInfo[] animClips = animator.GetCurrentAnimatorClipInfo(i);
    //        animClips[0].clip.
    //    }

    //}
}
